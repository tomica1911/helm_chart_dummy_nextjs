apiVersion: v1
kind: Service
metadata:
  name: {{ include "dummy-nextjs-chart.fullname" . }}
  labels:
    {{- include "dummy-nextjs-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dummy-nextjs-chart.selectorLabels" . | nindent 4 }}
